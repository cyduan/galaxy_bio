# Trusted local Galaxy tools whose generated HTML reports may be displayed as
# text/html without Galaxy converting them to plain text. Keep this list narrow:
# do not add tools that pass user-authored HTML through unchanged.
structure_quality_annotator
